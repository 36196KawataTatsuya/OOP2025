<Window x:Class="TenkiApp.LocationManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="地域管理" Height="500" Width="350"
        WindowStyle="ToolWindow" ShowInTaskbar="False"
        Background="#252526" Foreground="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="新しい都市を追加" Foreground="#AAAAAA" FontSize="14" Margin="0,0,0,8"/>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
            <Grid Width="240" Margin="0,0,10,0">
                <TextBox x:Name="SearchBox" Padding="8" FontSize="16" Background="#333337" Foreground="White" BorderThickness="0"
                         GotFocus="SearchBox_GotFocus" LostFocus="SearchBox_LostFocus" TextChanged="SearchBox_TextChanged"/>

                <TextBlock x:Name="SearchPlaceholder" Text="都市名を入力" Foreground="#777777" FontSize="16"
                           IsHitTestVisible="False" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </Grid>

            <Button Content="+" Width="50" Click="AddLocation_Click" Background="#007ACC" BorderThickness="0" Foreground="White" FontSize="20"/>
        </StackPanel>

        <ListView x:Name="LocationListView" Grid.Row="1" SelectionMode="Single" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="16">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Padding="5"/>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="Margin" Value="0,2"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}" BorderBrush="#3E3E42" BorderThickness="0,0,0,1">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#3E3E42"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="▲" Width="50" Margin="5" Click="MoveUp_Click" Background="#3E3E42" FontSize="16"/>
            <Button Content="▼" Width="50" Margin="5" Click="MoveDown_Click" Background="#3E3E42" FontSize="16"/>
            <Button Content="削除" Width="100" Margin="5" Click="Delete_Click" Background="#C42B1C" FontSize="16"/>
        </StackPanel>
    </Grid>
</Window>