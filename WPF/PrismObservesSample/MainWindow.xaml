<Window x:Class="PrismObservesSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismObservesSample"
        mc:Ignorable="d"
        Title="MainWindow" Height="120" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!-- 入力1 -->
        <TextBox x:Name="input1" Grid.Row="0" Grid.Column="0" Height="30" Margin="5"
                 Text="{Binding Input1}"/>
        <!-- +記号 -->
        <TextBlock Grid.Row="0" Grid.Column="1" Height="30" Margin="5"
                Text="+" TextAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
        <!-- 入力2 -->
        <TextBox x:Name="input2" Grid.Row="0" Grid.Column="2" Height="30" Margin="5"
                 Text="{Binding Input2}"/>
        <!-- =記号 -->
        <TextBlock Grid.Row="0" Grid.Column="3" Height="30" Margin="5"
                Text="=" TextAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
        <!-- 計算結果 -->
        <Border Grid.Row="0" Grid.Column="4" BorderBrush="Black" BorderThickness="1"
        CornerRadius="2" Height="30" Margin="5" VerticalAlignment="Top"
        Padding="5,0,5,0">
            <TextBlock x:Name="Result" Text="{Binding Result}" VerticalAlignment="Center" />
        </Border>
        <!-- 計算ボタン -->
        <Button Grid.Row="1" Grid.Column="4" Content="計算" Margin="4"
                    Command="{Binding SumCommand}"/>
        <!-- クリアボタン -->
        <Button Grid.Row="1" Grid.Column="2" Content="クリア" Margin="4"
            Command="{Binding Clear}"/>
    </Grid>
</Window>
