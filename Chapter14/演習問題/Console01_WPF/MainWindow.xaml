<Window x:Class="Console01_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Console01_WPF"
        mc:Ignorable="d"
        Title="テキストリーダー" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
            <Button x:Name="SelectFileButton" 
                    Content="テキストファイルを選択" 
                    Click="SelectFileButton_Click" 
                    HorizontalAlignment="Left" 
                    Padding="10,5"/>

            <CheckBox x:Name="TestModeCheckBox" 
                      Content="テストモード (読み込みに+5秒)"
                      VerticalAlignment="Center"
                      Margin="15,0,0,0"/>

            <Button x:Name="UiTestButton"
                    Content="UI応答テスト (ポップアップ)"
                    Click="UiTestButton_Click"
                    Margin="15,0,0,0"
                    Padding="10,5"
                    ToolTip="読み込み中にこのボタンが押せればUIは停止していません"/>
        </StackPanel>


        <TextBlock x:Name="StatusText" 
                   Grid.Row="1" 
                   Text="ファイルが選択されていません" 
                   Margin="0,10,0,5" 
                   FontStyle="Italic"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="FileContentText" 
                       TextWrapping="Wrap" 
                       Margin="5"
                       FontFamily="Consolas"
                       Background="#f4f4f4"/>
        </ScrollViewer>

    </Grid>
</Window>